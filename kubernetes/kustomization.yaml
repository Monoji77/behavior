apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Explicit allowlist: local secrets and infrastructure remain manually managed.
resources:
  - behavior-services.yaml
  - behavior-db-config.yaml
  - dashboard.yaml
