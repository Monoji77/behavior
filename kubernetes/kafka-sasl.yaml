# Template only. Copy to kafka-sasl.local.yaml, replace both values, then apply
# the local file. The local copy is ignored by Git.
apiVersion: v1
kind: Secret
metadata:
  name: kafka-sasl
  namespace: kafka
type: Opaque
stringData:
  username: CHANGE_ME_KAFKA_USERNAME
  password: CHANGE_ME_KAFKA_PASSWORD
