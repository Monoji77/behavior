# Template only. Copy to behavior-secrets.local.yaml, replace every CHANGE_ME
# value, then apply the local file. The local copy is ignored by Git.
apiVersion: v1
kind: Secret
metadata:
  name: behavior-secrets
  namespace: behavior
type: Opaque
stringData:
  POSTGRES_PASSWORD: CHANGE_ME_POSTGRES_PASSWORD
  INGESTION_COLLECTOR_TOKEN: CHANGE_ME_COLLECTOR_TOKEN
---
apiVersion: v1
kind: Secret
metadata:
  name: behavior-kafka-client
  namespace: behavior
type: Opaque
stringData:
  # Must match kafka-sasl.local.yaml exactly.
  username: CHANGE_ME_KAFKA_USERNAME
  password: CHANGE_ME_KAFKA_PASSWORD
